import Random from 'ember-theater-director-random/ember-theater/director/directions/random';
import { module, test } from 'qunit';

module('Unit | Ember Theater Direction | random');

// Replace this with your real tests.
test('it works', function(assert) {
  var random = Random.create();
  assert.ok(random);
});
